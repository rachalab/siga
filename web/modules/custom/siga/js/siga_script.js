(function ($, Drupal) {
    Drupal.behaviors.sigaBehavior = {
      attach: function (context, settings) {

      /*  $(context).find('input.my-input').once('sigaBehavior').each(function () {
          
        });*/
      }
    };
  })(jQuery, Drupal);
  